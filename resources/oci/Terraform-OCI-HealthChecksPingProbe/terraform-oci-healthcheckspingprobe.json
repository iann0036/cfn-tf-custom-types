{
    "typeName": "Terraform::OCI::HealthChecksPingProbe",
    "description": "This resource provides the Ping Probe resource in Oracle Cloud Infrastructure Health Checks service.\n\nCreates an on-demand ping probe. The location response header contains the URL for\nfetching probe results.\n\n*Note:* The on-demand probe configuration is not saved.",
    "sourceUrl": "https://github.com/iann0036/cfn-tf-custom-types.git",
    "definitions": {
        "Timeouts": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "Create": {
                    "type": "string"
                },
                "Delete": {
                    "type": "string"
                },
                "Update": {
                    "type": "string"
                }
            },
            "required": []
        }
    },
    "properties": {
        "tfcfnid": {
            "description": "Internal identifier for tracking resource changes. Do not use.",
            "type": "string"
        },
        "CompartmentId": {
            "type": "string",
            "description": "The OCID of the compartment."
        },
        "HomeRegion": {
            "type": "string"
        },
        "Id": {
            "type": "string"
        },
        "Port": {
            "type": "number",
            "description": "The port on which to probe endpoints. If unspecified, probes will use the default port of their protocol."
        },
        "Protocol": {
            "type": "string",
            "description": "The protocols for ping probes."
        },
        "ResultsUrl": {
            "type": "string"
        },
        "Targets": {
            "type": "array",
            "insertionOrder": false,
            "items": {
                "type": "string"
            },
            "description": "A list of targets (hostnames or IP addresses) of the probe."
        },
        "TimeCreated": {
            "type": "string"
        },
        "TimeoutInSeconds": {
            "type": "number",
            "description": "The probe timeout in seconds. Valid values: 10, 20, 30, and 60. The probe timeout must be less than or equal to `intervalInSeconds` for monitors."
        },
        "VantagePointNames": {
            "type": "array",
            "insertionOrder": false,
            "items": {
                "type": "string"
            },
            "description": "A list of names of vantage points from which to execute the probe."
        },
        "Timeouts": {
            "$ref": "#/definitions/Timeouts"
        }
    },
    "additionalProperties": false,
    "required": [
        "CompartmentId",
        "Protocol",
        "Targets"
    ],
    "readOnlyProperties": [
        "/properties/tfcfnid",
        "/properties/HomeRegion",
        "/properties/Id",
        "/properties/ResultsUrl",
        "/properties/TimeCreated"
    ],
    "primaryIdentifier": [
        "/properties/Id"
    ],
    "handlers": {
        "create": {
            "permissions": [
                "s3:PutObject",
                "secretsmanager:GetSecretValue"
            ]
        },
        "read": {
            "permissions": []
        },
        "update": {
            "permissions": [
                "s3:GetObject",
                "s3:PutObject",
                "secretsmanager:GetSecretValue"
            ]
        },
        "delete": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "secretsmanager:GetSecretValue"
            ]
        },
        "list": {
            "permissions": []
        }
    }
}
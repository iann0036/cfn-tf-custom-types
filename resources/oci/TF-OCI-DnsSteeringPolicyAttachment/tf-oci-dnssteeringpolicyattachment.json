{
    "typeName": "TF::OCI::DnsSteeringPolicyAttachment",
    "description": "This resource provides the Steering Policy Attachment resource in Oracle Cloud Infrastructure DNS service.\n\nCreates a new attachment between a steering policy and a domain, giving the\npolicy permission to answer queries for the specified domain. A steering policy must\nbe attached to a domain for the policy to answer DNS queries for that domain.\n\nFor the purposes of access control, the attachment is automatically placed\ninto the same compartment as the domain's zone.",
    "sourceUrl": "https://github.com/iann0036/cfn-tf-custom-types.git",
    "documentationUrl": "https://github.com/iann0036/cfn-tf-custom-types/blob/docs/resources/oci/TF-OCI-DnsSteeringPolicyAttachment/docs/README.md",
    "definitions": {
        "TimeoutsDefinition": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "Create": {
                    "type": "string"
                },
                "Delete": {
                    "type": "string"
                },
                "Update": {
                    "type": "string"
                }
            },
            "required": []
        }
    },
    "properties": {
        "tfcfnid": {
            "description": "Internal identifier for tracking resource changes. Do not use.",
            "type": "string"
        },
        "CompartmentId": {
            "type": "string"
        },
        "DisplayName": {
            "type": "string",
            "description": "(Updatable) A user-friendly name for the steering policy attachment. Does not have to be unique and can be changed. Avoid entering confidential information."
        },
        "DomainName": {
            "type": "string",
            "description": "The attached domain within the attached zone. `domain_name` is case insensitive."
        },
        "Id": {
            "type": "string"
        },
        "Rtypes": {
            "type": "array",
            "insertionOrder": false,
            "items": {
                "type": "string"
            }
        },
        "Self": {
            "type": "string"
        },
        "State": {
            "type": "string"
        },
        "SteeringPolicyId": {
            "type": "string",
            "description": "The OCID of the attached steering policy."
        },
        "TimeCreated": {
            "type": "string"
        },
        "ZoneId": {
            "type": "string",
            "description": "The OCID of the attached zone."
        },
        "Timeouts": {
            "$ref": "#/definitions/TimeoutsDefinition"
        }
    },
    "additionalProperties": false,
    "required": [
        "DomainName",
        "SteeringPolicyId",
        "ZoneId"
    ],
    "readOnlyProperties": [
        "/properties/tfcfnid",
        "/properties/CompartmentId",
        "/properties/Id",
        "/properties/Rtypes",
        "/properties/Self",
        "/properties/State",
        "/properties/TimeCreated"
    ],
    "primaryIdentifier": [
        "/properties/tfcfnid"
    ],
    "handlers": {
        "create": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "lambda:InvokeFunction"
            ]
        },
        "read": {
            "permissions": [
                "s3:GetObject"
            ]
        },
        "update": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "lambda:InvokeFunction"
            ]
        },
        "delete": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "lambda:InvokeFunction"
            ]
        },
        "list": {
            "permissions": [
                "s3:GetObject",
                "s3:ListBucket"
            ]
        }
    }
}
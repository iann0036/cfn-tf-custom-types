{
    "typeName": "Terraform::AWS::AppsyncResolver",
    "description": "Provides an AppSync Resolver.",
    "sourceUrl": "https://github.com/iann0036/cfn-tf-custom-types.git",
    "definitions": {
        "PipelineConfig": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "Functions": {
                    "type": "array",
                    "insertionOrder": false,
                    "items": {
                        "type": "string"
                    },
                    "description": "The list of Function ID."
                }
            },
            "required": []
        }
    },
    "properties": {
        "tfcfnid": {
            "description": "Internal identifier for tracking resource changes. Do not use.",
            "type": "string"
        },
        "ApiId": {
            "type": "string",
            "description": "The API ID for the GraphQL API."
        },
        "Arn": {
            "type": "string"
        },
        "DataSource": {
            "type": "string",
            "description": "The DataSource name."
        },
        "Field": {
            "type": "string",
            "description": "The field name from the schema defined in the GraphQL API."
        },
        "Id": {
            "type": "string"
        },
        "Kind": {
            "type": "string",
            "description": "The resolver type. Valid values are `UNIT` and `PIPELINE`."
        },
        "RequestTemplate": {
            "type": "string",
            "description": "The request mapping template for UNIT resolver or 'before mapping template' for PIPELINE resolver."
        },
        "ResponseTemplate": {
            "type": "string",
            "description": "The response mapping template for UNIT resolver or 'after mapping template' for PIPELINE resolver."
        },
        "Type": {
            "type": "string",
            "description": "The type name from the schema defined in the GraphQL API."
        },
        "PipelineConfig": {
            "type": "array",
            "insertionOrder": false,
            "items": {
                "$ref": "#/definitions/PipelineConfig"
            },
            "maxItems": 1
        }
    },
    "additionalProperties": false,
    "required": [
        "ApiId",
        "Field",
        "RequestTemplate",
        "ResponseTemplate",
        "Type"
    ],
    "readOnlyProperties": [
        "/properties/tfcfnid",
        "/properties/Arn",
        "/properties/Id"
    ],
    "primaryIdentifier": [
        "/properties/tfcfnid"
    ],
    "handlers": {
        "create": {
            "permissions": [
                "*"
            ]
        },
        "read": {
            "permissions": [
                "*"
            ]
        },
        "update": {
            "permissions": [
                "*"
            ]
        },
        "delete": {
            "permissions": [
                "*"
            ]
        },
        "list": {
            "permissions": [
                "*"
            ]
        }
    }
}
{
    "typeName": "TF::AWS::ApprunnerCustomDomainAssociation",
    "description": "Manages an App Runner Custom Domain association.\n\n~> **NOTE:** After creation, you must use the information in the `certification_validation_records` attribute to add CNAME records to your Domain Name System (DNS). For each mapped domain name, add a mapping to the target App Runner subdomain (found in the `dns_target` attribute) and one or more certificate validation records. App Runner then performs DNS validation to verify that you own or control the domain name you associated. App Runner tracks domain validity in a certificate stored in AWS Certificate Manager (ACM).",
    "sourceUrl": "https://github.com/iann0036/cfn-tf-custom-types.git",
    "documentationUrl": "https://github.com/iann0036/cfn-tf-custom-types/blob/docs/resources/aws/TF-AWS-ApprunnerCustomDomainAssociation/docs/README.md",
    "definitions": {
        "CertificateValidationRecordsDefinition": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "Name": {
                    "type": "string"
                },
                "Status": {
                    "type": "string"
                },
                "Type": {
                    "type": "string"
                },
                "Value": {
                    "type": "string"
                }
            }
        }
    },
    "properties": {
        "tfcfnid": {
            "description": "Internal identifier for tracking resource changes. Do not use.",
            "type": "string"
        },
        "CertificateValidationRecords": {
            "type": "array",
            "insertionOrder": true,
            "items": {
                "$ref": "#/definitions/CertificateValidationRecordsDefinition"
            }
        },
        "DnsTarget": {
            "type": "string"
        },
        "DomainName": {
            "type": "string",
            "description": "The custom domain endpoint to association. Specify a base domain e.g. `example.com` or a subdomain e.g. `subdomain.example.com`."
        },
        "EnableWwwSubdomain": {
            "type": "boolean"
        },
        "Id": {
            "type": "string"
        },
        "ServiceArn": {
            "type": "string",
            "description": "The ARN of the App Runner service."
        },
        "Status": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": [
        "DomainName",
        "ServiceArn"
    ],
    "readOnlyProperties": [
        "/properties/tfcfnid",
        "/properties/CertificateValidationRecords",
        "/properties/DnsTarget",
        "/properties/Id",
        "/properties/Status"
    ],
    "primaryIdentifier": [
        "/properties/tfcfnid"
    ],
    "handlers": {
        "create": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "lambda:InvokeFunction"
            ]
        },
        "read": {
            "permissions": [
                "s3:GetObject"
            ]
        },
        "update": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "lambda:InvokeFunction"
            ]
        },
        "delete": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "lambda:InvokeFunction"
            ]
        },
        "list": {
            "permissions": [
                "s3:GetObject",
                "s3:ListBucket"
            ]
        }
    }
}
{
    "typeName": "Terraform::Grafana::DataSource",
    "description": "The data source resource allows a data source to be created on a Grafana server.",
    "sourceUrl": "https://github.com/iann0036/cfn-tf-custom-types.git",
    "definitions": {
        "JsonData": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "AssumeRoleArn": {
                    "type": "string",
                    "description": "The role\nARN to be assumed by Grafana when using the CloudWatch data source."
                },
                "AuthType": {
                    "type": "string",
                    "description": "The authentication type\ntype used to access the data source."
                },
                "CustomMetricsNamespaces": {
                    "type": "string",
                    "description": "A comma-separated list of custom namespaces to be queried by the CloudWatch\ndata source."
                },
                "DefaultRegion": {
                    "type": "string"
                }
            },
            "required": [
                "AuthType",
                "DefaultRegion"
            ]
        },
        "SecureJsonData": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "AccessKey": {
                    "type": "string",
                    "description": "The access key required\nto access the data source."
                },
                "SecretKey": {
                    "type": "string",
                    "description": "The secret key required\nto access the data source."
                }
            },
            "required": [
                "AccessKey",
                "SecretKey"
            ]
        }
    },
    "properties": {
        "tfcfnid": {
            "description": "Internal identifier for tracking resource changes. Do not use.",
            "type": "string"
        },
        "AccessMode": {
            "type": "string",
            "description": "The method by which the browser-based Grafana\napplication will access the data source. The default is \"proxy\", which means\nthat the application will make requests via a proxy endpoint on the Grafana\nserver."
        },
        "BasicAuthEnabled": {
            "type": "boolean",
            "description": "- If true, HTTP basic authentication will\nbe used to make requests."
        },
        "BasicAuthPassword": {
            "type": "string",
            "description": "The\npassword to use for basic auth."
        },
        "BasicAuthUsername": {
            "type": "string",
            "description": "The\nusername to use for basic auth."
        },
        "DatabaseName": {
            "type": "string",
            "description": "The name of the\ndatabase to use on the selected data source server."
        },
        "Id": {
            "type": "string"
        },
        "IsDefault": {
            "type": "boolean",
            "description": "If true, the data source will be the default\nsource used by the Grafana server. Only one data source on a server can be\nthe default."
        },
        "Name": {
            "type": "string",
            "description": "A unique name for the data source within the Grafana\nserver."
        },
        "Password": {
            "type": "string",
            "description": "The password to use to\nauthenticate to the data source."
        },
        "Type": {
            "type": "string",
            "description": "The data source type. Must be one of the data source\nkeywords supported by the Grafana server."
        },
        "Url": {
            "type": "string",
            "description": "The URL for the data source. The type of URL required\nvaries depending on the chosen data source type."
        },
        "Username": {
            "type": "string",
            "description": "The username to use to\nauthenticate to the data source."
        },
        "JsonData": {
            "type": "array",
            "insertionOrder": false,
            "items": {
                "$ref": "#/definitions/JsonData"
            }
        },
        "SecureJsonData": {
            "type": "array",
            "insertionOrder": false,
            "items": {
                "$ref": "#/definitions/SecureJsonData"
            }
        }
    },
    "additionalProperties": false,
    "required": [
        "Name",
        "Type"
    ],
    "readOnlyProperties": [
        "/properties/tfcfnid",
        "/properties/Id"
    ],
    "primaryIdentifier": [
        "/properties/tfcfnid"
    ],
    "handlers": {
        "create": {
            "permissions": [
                "s3:PutObject",
                "secretsmanager:GetSecretValue"
            ]
        },
        "read": {
            "permissions": []
        },
        "update": {
            "permissions": [
                "s3:GetObject",
                "s3:PutObject",
                "secretsmanager:GetSecretValue"
            ]
        },
        "delete": {
            "permissions": [
                "s3:GetObject",
                "s3:DeleteObject",
                "secretsmanager:GetSecretValue"
            ]
        },
        "list": {
            "permissions": []
        }
    }
}